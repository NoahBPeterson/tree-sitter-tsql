=====================================
CREATE SCHEMA basic
=====================================

CREATE SCHEMA Sales;

-------------------------------------

(tsql_file
  (batch
    (sql_clauses
      (ddl_clause
        (create_schema
          (id_))))))

=====================================
CREATE SCHEMA with authorization
=====================================

CREATE SCHEMA HR AUTHORIZATION dbo;

-------------------------------------

(tsql_file
  (batch
    (sql_clauses
      (ddl_clause
        (create_schema
          (id_)
          (id_))))))

=====================================
CREATE TYPE from base type
=====================================

CREATE TYPE dbo.PhoneNumber FROM VARCHAR(20) NOT NULL;

-------------------------------------

(tsql_file
  (batch
    (sql_clauses
      (ddl_clause
        (create_type
          (full_table_name
            (id_)
            (id_))
          (data_type
            (varchar_)
            (decimal_))
          (null_notnull
            (null_)))))))

=====================================
CREATE TYPE from base type nullable
=====================================

CREATE TYPE dbo.OptionalName FROM NVARCHAR(100) NULL;

-------------------------------------

(tsql_file
  (batch
    (sql_clauses
      (ddl_clause
        (create_type
          (full_table_name
            (id_)
            (id_))
          (data_type
            (nvarchar_)
            (decimal_))
          (null_notnull
            (null_)))))))

=====================================
CREATE TYPE as table
=====================================

CREATE TYPE dbo.IdList AS TABLE (Id INT NOT NULL, Name NVARCHAR(100));

-------------------------------------

(tsql_file
  (batch
    (sql_clauses
      (ddl_clause
        (create_type
          (full_table_name
            (id_)
            (id_))
          (table_element
            (column_definition
              (id_)
              (data_type
                (int_))
              (null_notnull
                (null_))))
          (table_element
            (column_definition
              (id_)
              (data_type
                (nvarchar_)
                (decimal_)))))))))

=====================================
CREATE SEQUENCE basic
=====================================

CREATE SEQUENCE dbo.SeqOrder;

-------------------------------------

(tsql_file
  (batch
    (sql_clauses
      (ddl_clause
        (create_sequence
          (full_table_name
            (id_)
            (id_)))))))

=====================================
CREATE SEQUENCE full options
=====================================

CREATE SEQUENCE dbo.SeqFull
  AS INT
  START WITH 1
  INCREMENT BY 1
  MINVALUE 1
  MAXVALUE 1000
  CYCLE
  CACHE 10;

-------------------------------------

(tsql_file
  (batch
    (sql_clauses
      (ddl_clause
        (create_sequence
          (full_table_name
            (id_)
            (id_))
          (data_type
            (int_))
          (decimal_)
          (decimal_)
          (decimal_)
          (decimal_)
          (decimal_))))))

=====================================
CREATE SEQUENCE with NO options
=====================================

CREATE SEQUENCE dbo.SeqNoOpt
  START WITH 100
  INCREMENT BY 5
  NO MINVALUE
  NO MAXVALUE
  NO CYCLE
  NO CACHE;

-------------------------------------

(tsql_file
  (batch
    (sql_clauses
      (ddl_clause
        (create_sequence
          (full_table_name
            (id_)
            (id_))
          (decimal_)
          (decimal_))))))

=====================================
CREATE SYNONYM
=====================================

CREATE SYNONYM dbo.Emp FOR HumanResources.dbo.Employees;

-------------------------------------

(tsql_file
  (batch
    (sql_clauses
      (ddl_clause
        (create_synonym
          (full_table_name
            (id_)
            (id_))
          (full_table_name
            (id_)
            (id_)
            (id_)))))))

=====================================
TRUNCATE TABLE
=====================================

TRUNCATE TABLE dbo.TempData;

-------------------------------------

(tsql_file
  (batch
    (sql_clauses
      (ddl_clause
        (truncate_table
          (full_table_name
            (id_)
            (id_)))))))
